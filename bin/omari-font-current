#!/bin/bash

# Returns the name of the current monospace font being used by extracting it from the terminal config.
# This can be changed using omari-font-set.

grep -oP 'font-family:\s*["'\'']?\K[^;"'\'']+' ~/.config/waybar/style.css | head -n1
