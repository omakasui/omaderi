#!/bin/bash

# Overwrite the user configs for the Waybar menu bar with the Omari defaults and restart the service.

omari-refresh-config waybar/config.jsonc
#omari-refresh-config waybar/style.css
omari-restart-waybar
