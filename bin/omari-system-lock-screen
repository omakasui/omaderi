#!/bin/bash

# Lock the screen and ensure 1password is locked as well.

# Ensure 1password is locked
if pgrep -x "1password" >/dev/null; then
  1password --lock &
fi

# Lock the screen
loginctl lock-session