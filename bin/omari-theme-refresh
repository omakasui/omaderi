#!/bin/bash

# Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/omari/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  omari-theme-set "$(cat $THEME_NAME_PATH)"
fi
